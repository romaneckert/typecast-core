extends ../../../layout/typecast/signed-out

block append head
    title=view.translate(locale, 'typecast.user.password_success.title')
    meta(name="description" content=view.translate(locale, 'typecast.user.password_success.description'))

block content

    h1.h2=view.translate(locale, 'typecast.user.password_success.headline')

    .typecast-text
        p=view.translate(locale, 'typecast.user.password_success.text')

        a.btn.btn-primary(href="/typecast/user/sign-in")=view.translate(locale, 'typecast.user.password_success.button')
